# Supabsae based Custom CMS for NextJS website

## Summary

This projects purpose is to establish a simple content management system based on a connection to a supabsae database. The site should be able to display dtata form the database, but also update/add data to the defined tables in supabase.

- This is a fresh project with only a custom nextjs starter installed.
- I am reletively new to supabase connection so i will need walked through connection setup, as well as explanation and configuration of needed edge functions.
- This is an ambitious project that will take several hours to complete. I would suggest we started by aligning on a full-fleshed out plan than we can carry out over several sessions.
- Open to suggestions, and criticsum along the way.
- Let's start small, and add in functionality and specifcs after we have a baseline product.
- Eventually Authenication will need to be introduced, but we won't need this until much later in the project.
- Data should be accessed on the frontend using graphql.
- Utilize .env variables for connections.
- Make this as portbale as possible since it may end up being a template for projects going forward.
- We should include a dhasboard.
- We will utlize Radix Primitives where applicable, leaning on those components for functionality within our custom components.
